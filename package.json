{
  "name": "dyamond-geos-wind-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DYAMOND GEOS Wind Dashboard — servidor unificado (API + frontend)",
  "scripts": {
    "start": "node dist/index.mjs",
    "install-deps": "npm install"
  },
  "dependencies": {
    "pino-pretty": "^13.1.3",
    "thread-stream": "3.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
